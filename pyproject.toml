[project]
name = "suan-agent-v0"
version = "0.1.0"
description = "Deterministic V0 strategy evaluation loop"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
